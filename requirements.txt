# ============================================================
# requirements.txt — CPU-only OCR engine tester
# ============================================================
# Fresh install:
#   pip install --upgrade pip
#   pip install -r requirements.txt
# ============================================================

# --- PDF rendering & image handling -------------------------
PyMuPDF>=1.24.0
Pillow>=10.0.0

# --- Core utilities -----------------------------------------
numpy>=1.26.0

# --- OCR Engine: RapidOCR (default, fastest on CPU) ---------
rapidocr-onnxruntime>=1.3.0

# --- OCR Engine: Tesseract ----------------------------------
# Also requires the Tesseract binary installed on your OS:
#   Windows : https://github.com/UB-Mannheim/tesseract/wiki
#   Ubuntu  : sudo apt install tesseract-ocr
#   macOS   : brew install tesseract
pytesseract>=0.3.10

# --- OCR Engine: EasyOCR ------------------------------------
easyocr>=1.7.0

# --- OCR Engine: docTR --------------------------------------
python-doctr>=0.9.0
# docTR needs torch — CPU build (no CUDA, smaller download)
torch>=2.2.0
torchvision>=0.17.0

# --- OCR Engine: Surya OCR ----------------------------------
surya-ocr==0.16.7

# --- OCR Engine: PaddleOCR (CPU build only) -----------------
# Do NOT install paddlepaddle-gpu — it conflicts with torch on Windows
paddlepaddle>=3.1.1
paddleocr>=3.6.0